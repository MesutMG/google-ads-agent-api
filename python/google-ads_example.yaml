# Google Ads API Credentials
developer_token: "DEVELOPER_TOKEN" #from google ads manager account

client_id: "CLIENT_ID" #ends with .apps.googleusercontent.com from google cloud oauth
client_secret: "CLIENT_SECRET" #from google cloud
refresh_token: "REFRESH_TOKEN" #use generate refresh token to get it

login_customer_id: "MCC_ADS_ACCOUNT_ID" #from google ads manager account

use_proto_plus: true
